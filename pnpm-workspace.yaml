# Every workspace here is a publishable package.
packages:
  - packages/*

# Approve build scripts for trusted dependencies (pnpm 11+).
allowBuilds:
  lefthook: true

# Shared dependency versions — single source of truth across all packages.
# Add entries with `pnpm add <pkg> --save-catalog`.
catalog: {}
