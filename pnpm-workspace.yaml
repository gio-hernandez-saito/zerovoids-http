# Every workspace here is a publishable package.
packages:
  - packages/*

# Approve build scripts for trusted dependencies (pnpm 11+).
allowBuilds:
  esbuild: true # size-limit → esbuild installs its platform binary
  lefthook: true
